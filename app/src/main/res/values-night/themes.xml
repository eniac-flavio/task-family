<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Taskfamily" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- tema escuro. -->
        <item name="colorPrimary">@color/prim500</item>
        <item name="colorAccent">@color/acc500</item>
        <item name="colorSecondary">@color/sec500</item>
        <item name="android:colorBackground">@color/bg50</item>
    </style>
</resources>