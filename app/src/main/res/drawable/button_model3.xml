<?xml version="1.0" encoding="utf-8"?>
<!-- Sombra -->
<!--suppress ALL -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:bottom="10dp"
        android:right="10dp">
        <shape android:shape="rectangle">
            <solid android:color="#66000000" /> <!-- Sombra preta com transparência -->
            <corners android:radius="20dp" />
        </shape>
    </item>

    <!-- Botão principal -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/prim400" /> <!-- Cor do botão -->
            <corners android:radius="20dp" />
        </shape>
    </item>

</layer-list>
